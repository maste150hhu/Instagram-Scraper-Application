class UserProfile {

    constructor(name, bio, img, url, posts, followedBy, follow) {
        this.username = name;
        this.biography = bio;
        this.profilepicture = img;
        this.url = url;
        this.posts = posts;
        this.followedBy = followedBy;
        this.follow = follow;
    }
}