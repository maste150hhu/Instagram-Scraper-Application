class UserProfile {

    constructor(name, bio, img, url, posts) {
        this.username = name;
        this.biography = bio;
        this.profilepicture = img;
        this.url = url;
        this.posts = posts;
    }
}